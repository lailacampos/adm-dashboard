import Header from "../../components/Header"
import { SIDEBAR_ITEMS_PT_BR } from "../../data/sidebarItems";

const Orders = () => {
    return (
        <>
            <Header title={SIDEBAR_ITEMS_PT_BR.orders.name} />
        </>
    );
}

export default Orders