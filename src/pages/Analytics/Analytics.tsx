import Header from "../../components/Header"
import { SIDEBAR_ITEMS_PT_BR } from "../../data/sidebarItems";

const Analytics = () => {
    return (
        <>
            <Header title={SIDEBAR_ITEMS_PT_BR.analytics.name} />
        </>
    );
}

export default Analytics